name: data
publish_to: 'none'
version: 1.0.0+1

environment: 
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^7.2.0
  hive: ^2.0.5
  hive_flutter: ^1.1.0

  domain:
    path: ../../packages/domain/

dev_dependencies: 
  dart_code_metrics: ^4.9.1    
  flutter_lints: ^1.0.4

flutter:
  assets:
  - ../../assets/images/writers/
